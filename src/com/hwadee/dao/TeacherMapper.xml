<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hwadee.dao.TeacherMapper">


  
  <select id="getTeacherList"  resultType="com.hwadee.pojo.Teacher">
    select 
   		t_id,
		t_name,
		p_name,
		t_sex,
		t_age,
		t_birth,
		t_address,
		t_identity,
		t_nationality,
		entry_time,
		phone,
		salary
    from teacher 
    <if test="t_name!=null and t_name!=''">
    	where t_name = #{t_name}
	</if>
  </select>
  

  
  <delete id="deleteTeacher" parameterType="java.lang.String">
    delete from teacher
    where t_id = #{t_id}
  </delete>

  
  <insert id="insertTeacher" parameterType="com.hwadee.pojo.Teacher"> 

    insert into teacher (
    	t_id,
		t_name,
		p_name,
		t_sex,
		t_age,
		t_birth,
		t_address,
		t_identity,
		t_nationality,
		entry_time,
		phone,
		salary
      )
    values (#{t_id,jdbcType=VARCHAR},#{t_name,jdbcType=VARCHAR}, #{p_name,jdbcType=VARCHAR}, 
      #{t_sex,jdbcType=VARCHAR},#{t_age,jdbcType=VARCHAR},#{t_birth,jdbcType=VARCHAR},
      #{t_address,jdbcType=VARCHAR},#{t_identity,jdbcType=VARCHAR},#{t_nationality,jdbcType=VARCHAR},
      #{entry_time,jdbcType=VARCHAR},#{phone,jdbcType=VARCHAR},#{salary,jdbcType=VARCHAR}
      )
  </insert>

  <update id="updateTeacher" parameterType="com.hwadee.pojo.Teacher">
	  update teacher
	    set 
	     t_name=#{t_name,jdbcType=VARCHAR},
		p_name=#{p_name,jdbcType=VARCHAR},
		t_sex=#{t_sex,jdbcType=VARCHAR},
		t_age=#{t_age,jdbcType=VARCHAR},
		t_birth=#{t_birth,jdbcType=VARCHAR},
		t_address=#{t_address,jdbcType=VARCHAR},
		t_identity=#{t_identity,jdbcType=VARCHAR},
		t_nationality=#{t_nationality,jdbcType=VARCHAR},
		entry_time=#{entry_time,jdbcType=VARCHAR},
		phone=#{phone,jdbcType=VARCHAR},
		salary=#{salary,jdbcType=VARCHAR}
	   where t_id=#{t_id}
  </update>
  

  



</mapper>