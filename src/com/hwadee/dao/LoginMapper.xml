<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hwadee.dao.LoginMapper">

  <select id="findUser"  resultType="com.hwadee.pojo.User">
    select 	* from User where id = #{id} and password = #{password}

  </select>
  

  <insert id="insertUser" parameterType="com.hwadee.pojo.User"> 

    insert into User (
		id,
		password,
		authority
      )
    values (#{id}, #{password,jdbcType=VARCHAR}, 
      #{authority,jdbcType=VARCHAR})
  </insert>

 

</mapper>